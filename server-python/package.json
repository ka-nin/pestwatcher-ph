{
  "name": "server-python",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "python -m venv .venv && .venv\\Scripts\\pip install -r requirements.txt",
    "dev": ".venv\\Scripts\\uvicorn app.main:app --reload --port 8000",
    "start": ".venv\\Scripts\\uvicorn app.main:app --port 8000"
  }
}
