rice-pest-system/
├── .github/workflows/         # CI/CD pipelines (linting, tests, automated builds)
├── .gitignore
├── package.json               # Root workspace controller (npm/pnpm/yarn workspaces)
├── README.md
│
├── packages/                  # Shared internal packages
│   └── shared-types/          # TypeScript interfaces shared across web, mobile, and server
│       ├── src/
│       │   ├── pest.types.ts  # Risk levels, outbreak parameters
│       │   ├── user.types.ts  # Admin & farmer roles
│       │   └── weather.types.ts # Meteorological input schemas
│       └── package.json
│
├── server-python/             # Backend API & Machine Learning Integration (FastAPI)
│   ├── app/
│   │   ├── config.py          # Settings loaded from .env (pydantic-settings)
│   │   ├── routers/           # auth.py, weather.py, inference.py
│   │   ├── data/               # Hardcoded accounts today; swap for a real DB later
│   │   ├── schemas/            # Pydantic request/response models
│   │   ├── models/             # ResNet-50 / BiLSTM model wrappers (weights not yet trained)
│   │   ├── preprocessing/      # Engineered biological features (GDD, CRF, HP)
│   │   └── main.py             # FastAPI app, CORS, router registration
│   ├── requirements.txt
│   └── package.json           # npm workspace shim (dev/setup scripts shell out to Python)
│
├── admin-web/                 # Web Dashboard for Administrators (React + Vite + Tailwind)
│   ├── src/
│   │   ├── api/               # Axios/Fetch service hooks for backend communication
│   │   ├── components/        # Reusable UI elements (Tables, Charts, Modals)
│   │   ├── context/           # Auth and global dashboard states
│   │   ├── layouts/           # Sidebar, Navigation header layouts
│   │   ├── pages/             # Views (Overview, Pest Outbreak Logs, Model Metrics, User Control)
│   │   ├── router/            # Protected route configurations
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── vite.config.ts
│   └── package.json
│
└── user-mobile/               # Mobile Application for Farmers (React Native / Expo)
    ├── src/
    │   ├── api/               # Mobile client API services
    │   ├── components/        # Mobile-specific UI (Weather widgets, risk indicator cards)
    │   ├── navigation/        # Expo Router or React Navigation (Bottom Tabs, Stack)
    │   ├── screens/           # Views (Home/Risk Feed, Weather Outlook, Pest Guide, Profile)
    │   ├── store/             # Local state (Zustand or Redux Toolkit)
    │   └── App.tsx
    ├── app.json
    └── package.json